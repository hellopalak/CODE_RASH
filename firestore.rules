rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY: Allow all reads/writes for testing
    // Change this to 'if request.auth != null;' for production
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
